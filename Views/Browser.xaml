<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Shadler.Views.Browser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Shadler.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DropDownButton Content="Anime" HorizontalAlignment="Left">
            <DropDownButton.Flyout>
                <MenuFlyout Placement="Bottom">
                    <MenuFlyoutItem Text="Anime" />
                    <MenuFlyoutItem Text="Manga" />
                </MenuFlyout>
            </DropDownButton.Flyout>
        </DropDownButton>
        
        <AutoSuggestBox PlaceholderText="Search" QueryIcon="Find" Width="300" HorizontalAlignment="Right"/>

    </Grid>
</Page>
